# colours 사이트 운영 안내

## 시작하기 — 명령어 하나

VS Code 터미널(또는 PowerShell)에서:

```bash
cd C:\Users\USER\orca\projects\homepage
npm run cms
```

그러면 아래 **두 주소**가 함께 열립니다.

| | 주소 | 용도 |
|---|---|---|
| 🖥 **사이트** | **http://localhost:8080** | 실제 화면 확인 |
| ✏️ **관리자** | **http://localhost:8080/admin** | 글쓰기·상품 등록·설정 변경 |

브라우저 탭 두 개에 각각 띄워두고 쓰시면 편합니다.
관리자에서 저장 → 사이트 탭이 자동으로 새로고침됩니다.

**끄실 때**: 터미널에서 `Ctrl+C`

> 관리자에서 로그인 버튼이 뜨면 그냥 누르면 됩니다. 비밀번호 없습니다.
> (내 컴퓨터에서만 열리는 모드라 GitHub·Netlify 계정이 필요 없습니다)

---

# 0. 사이트 구조

```
홈 /              모든 글이 3:4 카드로 깔립니다. 위쪽 분류를 눌러 걸러 봅니다
글 /articles/…    카드를 누르면 열리는 읽는 화면
SHOP /shop/       인쇄판 목록
상품 /shop/vol1/  표지·사양·목차·주문 안내
ABOUT /about/     매체 소개
```

## 분류는 두 줄이 아니라 두 축입니다

홈 위쪽 분류는 **왼쪽이 형식, 오른쪽이 주제**입니다.

| | 항목 |
|---|---|
| **형식** (글의 종류) | Review · Interview · Curation · News · Essay |
| **주제** (무엇에 관한가) | FILM · CULTURE · ANIME · STYLE · TREND · PEOPLE · PLACE · 영화제 · 굿즈 · 출판 · 음악 |

`Review` + `FILM`을 함께 누르면 **둘 다 해당하는 글만** 남습니다.
같은 축 안에서 여러 개를 누르면 그중 아무거나 해당하면 보입니다.
`전체 보기`를 누르면 초기화됩니다.

---

# 1. 글쓰기

관리자 → 왼쪽 **`글`** → **`+ 글`**

| 항목 | 설명 |
|---|---|
| **제목** | `<호프>` 처럼 꺾쇠 그냥 쓰셔도 됩니다 |
| **부제** | 제목 아래 회색 한 줄. 비워도 됩니다 |
| **형식** | Review / Interview / Curation / News / Essay — **분류 왼쪽 축** |
| **주제 (대표)** | FILM / CULTURE / ANIME / … — **분류 오른쪽 축**. 하나만 |
| **주제 추가** | 여러 분류에 함께 걸고 싶을 때만. 보통 비워둡니다 |
| **발행일** | 홈 카드 순서가 이 순서입니다 |
| **글쓴이 · 사진 출처** | 글 아래 크레딧에 나옵니다 |
| **대표 이미지** | 끌어다 놓으면 업로드됩니다 |
| **본문** | 마크다운. 빈 줄로 문단을 나누세요 |
| **인스타그램 원문 주소** | 넣으면 글 아래에 인스타 카드가 생깁니다. 비우면 안 나옵니다 |
| **해시태그** | `#` 없이 단어만. 글 맨 아래에 붙습니다 |

오른쪽 위 **`게시됨`** 을 누르면 저장됩니다.
홈 카드와 분류 필터에 자동으로 들어갑니다. 따로 손댈 것 없습니다.

### 사진 준비

- **가로 1600px 이하, 500KB 이하**로 줄여서 올리세요 — <https://squoosh.app> (무료)
- 홈 썸네일은 **3:4 세로**로 잘립니다. 인물이면 얼굴이 위쪽 1/3에 오게

### 목록형 글 (영화 20편 / 책 10권처럼)

글 아래쪽 **`목록 (큐레이션용)`** → `Add 항목`

- **이름** — 작품명
- **부가 정보** — 예: `로버트 맥키 · 668p`
- **설명** — 한 줄 소개

형식은 **Curation**으로 두시면 분류에서 함께 묶입니다.

### ⚠️ 기존 글을 고칠 때 주의

목록에서 글을 눌러 내용을 바꾸면 **그 글이 덮어써집니다.**
새 글을 쓰실 때는 반드시 **`+ 글`** 버튼을 쓰세요.

---

# 2. SHOP 상품 등록

관리자 → 왼쪽 **`SHOP 상품`** → **`+ 상품`**

| 항목 | 설명 |
|---|---|
| **상품명** | 목록과 상세 페이지 제목 |
| **호수 (숫자만)** | `2` 를 넣으면 주소가 `/shop/vol2/` 가 됩니다 |
| **대표 이미지** | SHOP 목록에 뜨는 표지 |
| **상세 이미지** | 첫 장이 크게, 나머지는 아래 썸네일. 표지·내지·포스터 순을 권합니다 |
| **가격 · 배송비** | 둘을 더한 값이 `결제 예정 금액`에 자동으로 나옵니다 |
| **판형·분량·제본·구성·ISBN** | 사양 표 |
| **주문 링크** | **아래 설명 참고** |
| **입금 계좌 · 예금주** | 계좌 이체 안내에 쓰입니다 |
| **소개글 (왼쪽/오른쪽 단)** | 문단 하나씩 나눠서 넣으세요. 두 단으로 나란히 놓입니다 |
| **목차** | 한 줄에 한 편씩. 번호는 자동 |
| **품절** | 켜면 `주문하기`가 잠기고 목록에 품절로 표시됩니다 |

## 주문은 지금 어떻게 되나요

**주문 링크를 비워두면** — `주문하기`를 누르면 계좌 이체 안내가 펼쳐집니다.
입금하고 인스타 DM으로 주소를 보내는 방식입니다. 지금은 이 상태입니다.

**스마트스토어를 여시면** — 그 상품 주소를 `주문 링크`에 붙여 넣으세요.
그때부터 `주문하기`가 곧장 네이버페이 결제로 넘어갑니다. **고칠 곳은 이 칸 하나뿐입니다.**

> 이 사이트는 정적 사이트라 돈을 직접 받을 수 없습니다.
> (주문번호 발급·결제 승인·환불은 서버와 PG 심사가 필요합니다)
> 계좌 이체 → 스마트스토어 순으로 넘어가는 게 가장 현실적입니다.

---

# 3. 설정 바꾸기

관리자 → 왼쪽 **`사이트 설정`**

- **매체명 · 제호 옆 작은 글자** — `colours` + `영화` → `colours(영화)`
- **소개 문장** — 제호 바로 아래 회색 문장, 검색 결과에도 쓰입니다
- **인스타그램 주소** — 위쪽 메뉴의 INSTAGRAM이 여기로 연결됩니다
- **형식 분류 · 주제 분류** — 홈 위쪽 칩 목록

> ⚠️ **형식 분류**를 고치실 때는 `src/admin/config.yml` 의 글 → `형식` 선택지도
> 같이 맞춰주셔야 합니다. 두 곳이 어긋나면 필터에 걸리지 않는 글이 생깁니다.

---

# 4. 디자인 고치기

디자인은 관리자에 없습니다. **`src/style.css`** 를 VS Code로 직접 엽니다.

### 색·여백 — 파일 맨 위 `:root`
```css
--paper:#fff;      /* 지면 */
--ink:#000;        /* 글자·강조 */
--grey:#8c8c8c;    /* 보조 텍스트 */
--rule:#e2e2e2;    /* 실선 */
--edge:.5rem;      /* 화면 끝까지 꽉 채우는 바깥 여백 */
--maxw:1180px;     /* 상품 상세 최대 너비 */
```

### 자주 고치는 곳
```css
.grid{grid-template-columns:repeat(6,1fr)}   /* 홈 카드 한 줄 개수 */
.chip{font-size:clamp(.95rem,1.15vw,1.15rem)} /* 분류 칩 크기 */
.art{max-width:760px}                         /* 글 사진 너비 */
.col{max-width:520px}                         /* 글 본문 너비 */
```

저장하면 사이트 탭이 자동으로 새로고침됩니다.

---

# 5. 폴더 구조

```
homepage/
├── src/
│   ├── articles/       글      ← 관리자가 여기에 씁니다
│   ├── products/       SHOP 상품 ← 관리자가 여기에 씁니다
│   ├── _data/          설정    ← 관리자가 여기에 씁니다
│   ├── assets/         사진    ← 관리자가 여기에 올립니다
│   ├── style.css       디자인   ← 직접 수정
│   ├── index.njk       홈 구조
│   ├── shop.njk        SHOP 목록 구조
│   ├── about.njk       소개 페이지
│   ├── _includes/      틀        ⛔ 건드리지 마세요
│   └── admin/          관리자 설정 ⛔ 건드리지 마세요
├── _site/              자동 생성물 ⛔ 건드리지 마세요
└── .eleventy.js        빌드 설정   ⛔ 건드리지 마세요
```

---

# 6. 백업

관리자로 고치다 실수하면 되돌릴 방법이 필요합니다.
가장 쉬운 건 **폴더 통째로 복사**입니다.

```
homepage  →  homepage_백업_0826
```

`node_modules`와 `_site`는 빼고 복사해도 됩니다 (자동 생성물입니다).

---

# 7. 인터넷에 올리기 (남에게 보여줄 때)

지금은 **내 컴퓨터에서만** 열립니다. 공개하려면 한 번만 설정하면 됩니다.

| | 로컬 (`npm run cms`) | 배포 후 |
|---|---|---|
| 관리자 페이지 | ✅ | ✅ |
| 내가 확인 | ✅ | ✅ |
| **남이 접속** | ❌ | ✅ |
| **폰에서 관리** | ❌ | ✅ |

## STEP 1 — GitHub 계정 + 저장소
<https://github.com> 가입 → `New repository` → 이름 `colours` → **Private** → 생성

```bash
cd C:\Users\USER\orca\projects\homepage
git add .
git commit -m "colours 사이트"
git branch -M main
git remote add origin https://github.com/내계정이름/colours.git
git push -u origin main
```

## STEP 2 — Netlify 연결
<https://netlify.com> 가입 → `Add new site` → `Import an existing project` → GitHub → `colours`
빌드 설정은 자동으로 채워집니다. 그대로 `Deploy`.

## STEP 3 — 관리자 로그인 켜기
Netlify 사이트 화면에서:
1. `Site configuration` → `Identity` → **`Enable Identity`**
2. `Registration` → **`Invite only`** ← **꼭 바꾸세요.** 안 하면 아무나 가입해서 글을 씁니다
3. `Services` → `Git Gateway` → **`Enable Git Gateway`**
4. `Identity` → `Invite users` → 본인 이메일 → 메일에서 비밀번호 설정

이후에는 `사이트주소/admin` 으로 어디서든 접속됩니다.

---

# 아직 안 채워진 것

- [ ] 인스타그램 계정 주소 *(관리자 → 사이트 설정)*
- [ ] 스마트스토어 주문 링크 *(관리자 → SHOP 상품 → 주문 링크)*
- [ ] 글 사진 크레딧 *(각 글 편집 화면)*
- [ ] STYLE · PLACE · ANIME 주제의 글
- [ ] 모바일에서 확인

---

# 막히면

설치·배포 과정에서 막히거나 구조를 바꾸는 큰 작업이면 말씀해주세요.
글쓰기·상품 등록·설정 변경은 위 내용으로 충분합니다.
